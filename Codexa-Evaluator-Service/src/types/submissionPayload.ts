export type SubmissionPayload = {
    code: string,
    language: string,
    inputCase: string,
    outputCase: string,
    userId: string,
    submissionId: string
}
/**
 * Submission ka Payload hai ye basically as it is necessary while sending request
 */